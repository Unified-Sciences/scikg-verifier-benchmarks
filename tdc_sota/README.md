# TDC headline-result reproductions

This bundle contains final held-out predictions and labels for Ames, drug half-life, human intestinal absorption, and AqSolDB across five official seeds. Run `python reproduce_metrics.py` to verify every reported metric. It contains outputs, not the private evidence graph or verifier implementation.
